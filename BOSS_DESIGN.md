# Stage3 보스전 기획안

## 1. 개요

보스는 2페이즈 구조로 기획됨.

- **1페이즈**: 얼굴이 있는 귀여운 형태 (유니콘 인형을 안고 있는 소녀, `boss-idle-8frames`/`boss-claw-8frames` 아트). 아직 게임에 연결 안 됨, 향후 작업.
- **2페이즈**: 지금 실제로 구현되어 있는 검은 그림자 실루엣 형태. **이 문서는 2페이즈 전투 설계를 다룸.**

보스 본체는 **직접 공격할 수 없고 가만히 서 있음**. 플레이어는 위쪽 발판으로 올라가 보스의 약점인 "촉수"를 손전등으로 비춰 드러낸 뒤 총으로 파괴해야 보스에게 데미지가 들어감.

## 2. 전투 흐름

1. **컷신**: Stage3 시작 시 자동 재생 (`Stage3BossIntroCutscene.cs`). 플레이어가 몇 걸음 걸어가고, "?" 표시 → 카메라가 보스 쪽으로 이동 → 대사 → 플레이어 쪽 대사 → 다시 보스 쪽 → 촉수 할퀴기 연출 → 마지막 대사 → 조작권 반환.
2. **접근**: 보스는 스테이지 바닥(Ground)에 서 있음. 플레이어는 걸어서 접근.
3. **등반**: 보스 옆에 세워진 벽차기 전용 기둥(세로 벽 두 개)을 지그재그로 벽차기해서 위로 올라감.
4. **전투 발판 도달**: 꼭대기의 작은 전투 발판(Ground)에 촉수 3개(좌/중/우)가 배치되어 있음.
5. **촉수 처치**: 손전등으로 촉수를 비추면 실루엣이 사라지고 본모습이 드러나며 피격 가능 상태가 됨 → 총으로 맞추면 한 방에 파괴됨 → 보스 체력 2 감소 (피격 시 붉은 플래시) → 해당 촉수는 12초 후 재등장.
6. **반복**: 촉수 3개를 전부 파괴하면 2×3=6 = 보스 최대체력과 정확히 일치 → 재등장을 기다릴 필요 없이 한 번에 클리어 가능.
7. **보스 처치**: 체력 0 → 디졸브 애니메이션 재생 후 사라짐. (처치 이후 화면 전환/결과 연출은 아직 미정 — 별도 논의 필요.)

플레이어를 방해하는 요소로 등반 경로 중간에 숨겨진 원거리 몬스터(실루엣 상태로 숨어있다가 플레이어에게 접근, 손전등으로 비추면 드러나 총으로 처치 가능)가 배치되어 있음.

## 3. 지형 규칙 (중요)

레벨을 이루는 지오메트리는 용도에 따라 완전히 분리되어 있음 — **모양이 아니라 레이어로 구분**.

| 구분 | 레이어 | 용도 | 사용 에셋 |
|---|---|---|---|
| **벽차기 전용 벽** | `Wall` | 오직 벽차기(벽에 붙어서 점프)로만 상호작용. 위에 서거나 걸을 수 없음(의도적으로 Ground 체크에서 제외됨). | `WallVertical.png` — 세로로 긴 전용 벽 에셋. 자연스러운 비율 그대로 세로로 이어붙여서 사용 (억지로 늘리지 않음). |
| **평평한 바닥/발판** | `Ground` | 플레이어가 정상적으로 서고 걷는 모든 바닥 — 메인 스테이지 바닥, 등반 꼭대기의 전투 발판 등. | `FloorLarge.png` 등 발판 에셋. |

- 예전 시도에서 기울어진 발판 조각을 `Wall` 레이어로 겸용했더니 점프/걷기 애니메이션이 부자연스러워지는 문제가 있었음 (Ground 체크가 안 돼서 계속 "공중에 뜬 상태"로 인식됨). → **이후로는 절대 겸용하지 않음.** 벽차기가 필요한 구간은 반드시 `WallVertical` 전용 에셋으로 세로 벽을 만들고, 서 있어야 하는 곳은 반드시 `Ground` 레이어의 평평한 발판으로 만든다.
- `PlayerMovement2D.wallLayer` / `groundLayer`가 각각 이 두 레이어만 감지하도록 되어 있어서, 같은 오브젝트가 두 역할을 동시에 하는 것은 불가능함 (레이어는 오브젝트당 하나).

## 4. 핵심 컴포넌트

- `Boss2D.cs` — 체력(기본 6), `TakeDamage()`, 피격 플래시, 사망 시 디졸브.
- `BossTentacle2D.cs` — 손전등으로 실루엣→본모습 전환, 총 맞으면 파괴 + `boss.TakeDamage()` 호출 + 재등장 타이머.
- `RangedMonster2D.cs` — `useSilhouetteVisual` 옵션(신규 몬스터 전용, 기존 스테이지 몬스터는 미적용), 플레이어에게 접근하는 이동 로직 추가됨.
- `Stage3BossIntroCutscene.cs` — 보스 조우 컷신.
- `DevModeStage3.cs` — 테스트 편의용 (손전등 즉시 지급, 컷신 스킵 옵션).

## 5. 실패 사례 — 벽차기 구간 배치/크기

**컨셉 자체는 확정**: 보스 위쪽 공중에 벽/발판을 배치해서 플레이어가 타고 올라가 촉수를 때리는 구조는 좋음. **문제는 그 벽/발판을 "어디에, 얼마나 크게" 배치하느냐임.**

- **시도**: `WallVertical` 에셋으로 얇은 쌍둥이 기둥 두 개를 세워서 그 사이를 지그재그로 벽차기하며 올라가는 구조.
- **실패**: 기둥이 젓가락처럼 너무 얇고 딱 붙어있어서, 플레이어가 애초에 그 두 기둥 "사이"로 들어가지도 못하는 배치가 됨. 벽차기는 유저가 두 벽 사이 공간에 먼저 들어와 있어야 성립하는데, 진입 자체가 막히면 의미가 없음.
- **교훈**: 
  - 벽 사이 간격은 플레이어가 바닥에서 자연스럽게 걸어 들어갈 수 있는 "입구"가 있어야 함 — 두 벽을 딱 붙여서 좁은 틈만 만들면 안 됨.
  - 벽 두께/배치가 젓가락처럼 빈약하면 안 되고, 유저가 눈으로 봤을 때 "여기 들어가서 좌우로 차고 올라가면 되겠다"는 게 직관적으로 보여야 함.
  - 다음에 다시 만들 때는: (1) 진입 지점을 먼저 정하고 (2) 벽 간격을 유저가 걸어 들어갈 수 있을 만큼 확보한 뒤 (3) 벽 두께/높이를 잡는 순서로 배치할 것.

## 6. 현재 미정 사항

- **벽차기 구간을 정확히 어디에 배치하고, 벽/발판 크기를 어느 정도로 조정할지 아직 고민 중.** (위 5번 실패 사례로 인해 재설계 필요 — 진입 지점, 간격, 두께 전부 다시 잡아야 함)
- 보스 처치 후 연출/씬 전환 (아직 아무 것도 없음, 지금은 그냥 사라지기만 함).
- 1페이즈(얼굴 있는 형태) 연결 방식 — 언제/어떻게 2페이즈로 전환되는지.
